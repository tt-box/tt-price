{
  "name": "tt-price",
  "version": "0.0.1",
  "description": "Cash register Price",
  "main": "index.html",
  "repository": {
    "type": "git",
    "url": "https://github.com/tt-box/tt-price.git"
  },
  "directories": {
    "test": "test"
  },
  "scripts": {
    "lint": "polymer lint --input tt-price.html",
    "dev": "polymer serve -p 8080 & npm run watch",
    "test": "polymer test",
    "watch": "browser-sync start --proxy localhost:8080 --startPath '/components/tt-price/demo/' --files '*.html, *.js, demo/*' "
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.16.0"
  }
}
